{
  "name": "fandom-entry-pass",
  "version": "1.0.0",
  "description": "- Frontend PWA: index.html + manifest + service-worker + icons",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": true,
  "dependencies": {
    "stripe": "^16.6.0",
    "pg": "^8.11.0"
    "engines": { "node": ">=18" },        // matches our vercel runtime
  "scripts": {
    "vercel-build": "echo 'no build step'" // silences “no build” warnings
  }
}
